const math = require('./math');

console.log(math);

console.log(math.add(5, 6));

console.log(math.multiply(10, 20));

// built in module

// to handle filesystem fs

const fs = require('fs');

console.log(fs);

const http = require('http');

console.log(http);

// third party module
